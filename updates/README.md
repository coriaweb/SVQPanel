# SVQPanel — Sistema de Updates

Cada archivo `NNNN-descripcion.sh` es una migración numerada que `update.sh`
aplica una sola vez en cada servidor instalado.

## Reglas para crear un update

1. **Nombre**: `NNNN-descripcion-corta.sh` — NNNN es el siguiente número libre (4 dígitos, con ceros)
2. **Idempotente**: debe ser seguro de re-ejecutar si falla a mitad. Usa `|| true`, `if ! ...`, `--force`, etc.
3. **`exit 0`** al final si todo fue bien. Cualquier otro exit code detiene la cadena.
4. **No interactivo**: sin `read`, sin prompts. Se ejecuta a las 3am sin terminal.
5. **Loguea lo que hace**: usa `echo` para dejar rastro en `/var/log/svqpanel-update.log`.
6. Haz **commit + push** — los servidores lo descargan en la próxima ejecución del cron.

## Ejemplo de update

```bash
#!/bin/bash
# 0002-ejemplo-cambio.sh
# Descripción: qué hace y por qué

echo "→ Aplicando 0002: ejemplo de cambio..."

# Idempotente: comprueba antes de actuar
if ! grep -q "nueva_directiva" /etc/nginx/nginx.conf; then
    echo "nueva_directiva on;" >> /etc/nginx/nginx.conf
    nginx -t && systemctl reload nginx
    echo "✓ Directiva añadida"
else
    echo "  Ya estaba aplicado, nada que hacer."
fi

exit 0
```

## Historial de updates

| ID   | Descripción                          | Fecha      |
|------|--------------------------------------|------------|
| 0001 | Nginx desde repo oficial + HTTP/3    | 2026-06-04 |
| 0002 | PostgreSQL PGDG (PG15 → PG18)        | 2026-06-04 |
| 0003 | Terminal web — jaula chroot clientes | 2026-06-08 |
| 0004 | Terminal web — fix jaula (pts/binarios) | 2026-06-08 |
| 0005 | Terminal web — jaula por usuario        | 2026-06-08 |
| 0006 | Terminal web — prompt + bienvenida      | 2026-06-08 |
| 0007 | Terminal web — /proc hidepid (procesos) | 2026-06-08 |
| 0008 | Backup scheduler interno (sin timer 1/min) | 2026-06-08 |
| 0009 | Backups con restic (incremental+cifrado)   | 2026-06-09 |
| 0010 | Fix planificador backups (TZ + no morir)   | 2026-06-09 |
| 0011 | Zona horaria → reiniciar servicios de logs | 2026-06-09 |
| 0012 | Métricas: hilo interno (sin timer 5 min)   | 2026-06-09 |
| 0013 | Salud DNS: hilo interno (sin timer 10 min) | 2026-06-09 |
| 0014 | Fix jail Fail2ban postfix-sasl (login SMTP) | 2026-06-09 |
| 0015 | Sistema de licencias del panel             | 2026-06-09 |
| 0016 | Rspamd usa resolver DNS local (DNSBL)      | 2026-06-09 |
| 0017 | named modo IPv4 si no hay IPv6 (auto-rev.) | 2026-06-09 |
| 0018 | nginx listen genérico (enrutado vhosts)    | 2026-06-09 |
| 0019 | nginx max_headers (mitiga HTTP/2 Bomb)     | 2026-06-09 |
| 0020 | Web: gzip global + cache de estáticos     | 2026-06-09 |
| 0021 | Acceso remoto MySQL (allowlist IPs 3306)  | 2026-06-09 |
| 0022 | Exponer docs API (Swagger/ReDoc/OpenAPI)  | 2026-06-10 |
| 0023 | Sincronizar estado SSL del panel en la BD  | 2026-06-11 |
| 0024 | Limpiar vhosts huérfanos nginx/Apache       | 2026-06-23 |
| 0025 | Dominio canónico: forzar www (defensivo DNS) | 2026-06-23 |
| 0026 | Backfill DNS: ip_address + AAAA/ip6 en SPF   | 2026-06-23 |
| 0027 | Endurecer cifrados TLS (AEAD + prefer order) | 2026-06-23 |
| 0028 | Fix listen atado a IP en vhosts webmail/mail | 2026-06-23 |
| 0029 | TLS: algoritmos de firma SHA-256+ (sin SHA-224) | 2026-06-23 |
| 0030 | CAA Let's Encrypt en zonas DNS (issue+issuewild) | 2026-06-23 |
| 0031 | IP de salida SMTP por dominio con IPv6 (pref)  | 2026-06-23 |
| 0032 | Fix permisos home 750→711 (403 Forbidden web)  | 2026-06-23 |
| 0033 | Límite correo no autenticado (sitios hackeados) | 2026-06-24 |
| 0034 | Bajar límite no-auth a 10/h (empujar a SMTP)    | 2026-06-24 |
| 0035 | Aprendizaje de spam (Bayes + IMAPSieve Junk)   | 2026-06-24 |
| 0036 | Aprendizaje spam también por flag (Thunderbird) | 2026-06-24 |
| 0037 | SMTPS puerto 465 (SSL/TLS directo, además de 587) | 2026-06-24 |
| 0038 | Carpetas correo visibles en Thunderbird (auto-sub) | 2026-06-24 |
| 0039 | Auto-actualizaciones de seguridad del SO (unattended)| 2026-06-24 |
| 0040 | Endurecer servicios (banner SMTP/VRFY, BIND version)| 2026-06-24 |
| 0041 | Protección anti zip-bomb del antispam (Rspamd)      | 2026-06-24 |
| 0042 | Fix IPv6: netplan→networkd + ruta persistente (red rota al reiniciar) | 2026-06-25 |
| 0043 | Apache DirectoryIndex: index.php antes que index.html  | 2026-06-25 |
| 0044 | Página de suspensión escucha en IPv6 (cert válido)    | 2026-06-25 |
| 0045 | proxy_read_timeout /api/ a 1800s (504 en migraciones) | 2026-06-25 |
| 0046 | Carpeta de staging de migraciones (reutilizar backup) | 2026-06-25 |
| 0047 | GeoIP (países) en estadísticas de dominio + cron mensual | 2026-06-25 |
| 0048 | Informes de GoAccess en español (locale es_ES.UTF-8)   | 2026-06-26 |
| 0049 | Fix cluster DNS: declarar zonas nuevas en el slave     | 2026-06-26 |
| 0050 | Seguridad: cerrar recursión DNS (open resolver/DDoS)  | 2026-06-26 |
| 0051 | Instalar daemon cron (no corría ningún cronjob)       | 2026-06-26 |
| 0052 | Historial de ejecuciones de cron (wrapper + cola)     | 2026-06-26 |
| 0053 | Project quota: el correo cuenta en el disco del user  | 2026-06-26 |
| 0054 | Control de greylisting (global + por dominio)         | 2026-06-26 |
| 0055 | fail2ban: arreglar jail dovecot (aggressive) + jail postfix relay | 2026-06-26 |
| 0056 | Mover el spam marcado (X-Spam: Yes) a la carpeta Junk (Sieve global) | 2026-06-26 |
| 0057 | Unificar carpeta de spam en 'Junk' (Roundcube + migrar Spam→Junk + reentrenar) | 2026-06-26 |
| 0058 | Antispam: Bayes autoequilibrado (autolearn ham 0.5) + overrides admin (pesos/umbrales/reglas) | 2026-06-27 |
| 0059 | Umbral antispam por dominio hereda el global del admin (NULL = no personalizado) | 2026-06-27 |
| 0060 | Reglas antispam del admin a multimap.conf (un .conf aparte se ignoraba) + fix escape '/' y modo exacto/contiene | 2026-06-27 |
| 0061 | Resolver DNS propio (unbound localhost:5353) para Rspamd: reactiva SPF/DKIM/DMARC/RBL (el named del cluster los rechazaba) | 2026-06-27 |
| 0062 | Regenera config de correo de Dovecot en sintaxis 2.4 (quota + spam-learn + spam-junk + SNI) tras upgrade a Debian 13; no-op en Dovecot 2.3 | 2026-06-27 |
| 0063 | Reenvíos seguros: SRS (postsrsd) reescribe el envelope-from a @mydomain + fija la IP de salida del servidor (smtp_bind_address/6) + genera DKIM del dominio del servidor. Evita rebotes/blacklist al reenviar a Gmail/Outlook | 2026-06-27 |
| 0064 | SRS solo reescribe reenvíos: excluye los dominios locales (SRS_EXCLUDE_DOMAINS) para no tocar el correo propio (formularios PHP, buzón→buzón). Se mantiene sincronizado al crear/borrar dominios | 2026-06-27 |
| 0065 | fail2ban: jail para bots "lentos" de relay (Relay access denied, ventana 6h) que esquivan la jail postfix normal | 2026-06-29 |
| 0068 | Fix journalmatch Postfix en fail2ban (D13 usa postfix.service, no postfix@-.service): las jails de correo estaban ciegas | 2026-06-29 |
| 0069 | Antispam: marcar correo con cuerpo en cirílico → Junk (spam de formularios web ES y spam ruso) | 2026-06-29 |
| 0066 | Importación de backups en subproceso aislado + tar en streaming (no OOM); marca como fallidas las migraciones zombie en 'running' de la versión anterior | 2026-06-29 |
| 0067 | Temporales de migración a /var/lib/svqpanel/migration-tmp (disco real, no /tmp/tmpfs que se llenaba con backups grandes) + limpia huérfanos de varios GB de migraciones muertas por OOM | 2026-06-29 |
| 0070 | Fix HTTP/3: quitar `reuseport` del listen quic (rompía nginx con "duplicate listen options for 0.0.0.0:443" al 2º dominio con HTTP/3 → bloqueaba reloads y migraciones); regenera todos los vhosts | 2026-06-30 |
| 0071 | Subir límite de subida del gestor de archivos (100→2048 MB) y de extracción (500→5120 MB); demasiado bajo para un WordPress. Solo toca filas en el default viejo | 2026-06-30 |
| 0072 | Geo-bloqueo también cubre IPv6 (ipdeny v4+v6); refresca las listas geo_* existentes para que bloquear un país filtre además sus rangos IPv6 | 2026-06-30 |
| 0073 | Bloquear bots de robo de credenciales cloud (Silvy X Ran) + escáneres (LeakIX/Censys/Expanse) en el catálogo de bad-bots; respeta la selección del admin | 2026-06-30 |
| 0074 | Fix bad-bots.conf: entrecomillar patrones con espacios ("Silvy X Ran") que rompían el map de nginx (nginx -t fallaba → no recargaba); regenera y recarga | 2026-06-30 |
| 0075 | El catálogo global de bots ahora SÍ aplica a los vhosts nginx (antes el map $bad_bot no lo leía nadie): if ($bad_bot) return 444 en cada server + crea bad-bots.conf base + regenera vhosts | 2026-06-30 |
| 0076 | Fix: cmd_regenerate_all_vhosts generaba vhost web a dominios solo-correo/DNS (mail_dns_only) → public_html inexistente → configtest Apache fallaba y tumbaba la regeneración de TODOS. Limpia los vhosts ya creados | 2026-06-30 |
| 0077 | Cachear peso en disco de los dominios en BD (la lista hacía du en vivo por cada uno → lenta); refresco 2/día en background + botón por dominio. Primer cálculo | 2026-06-30 |
| 0078 | Quitar jail fail2ban relay redundante: el relay denied es spam distribuido (fail2ban no puede); ya lo cubre CrowdSec (postfix-relay-denied) | 2026-07-01 |
| 0079 | SSH + fail2ban estricto: bantime.increment global (baneo escalado x2 hasta 4 semanas para reincidentes) + jail sshd maxretry 3/findtime 30m/bantime 12h + sshd MaxAuthTries 3/LoginGraceTime 20 | 2026-07-01 |
| 0080 | fail2ban dbpurgeage = 5w (fail2ban.local): el historial de reincidencias se purgaba cada 1d y el escalado de 0079 nunca llegaba a semanas; ahora persiste > maxtime | 2026-07-01 |
| 0081 | Protección anti fuerza bruta WordPress por dominio (xmlrpc 444 + rate-limit wp-login 3/min); crea columnas + protege los dominios bajo ataque (deja cronicasliterarias.es sin proteger para validar el aviso) | 2026-07-01 |
| 0082 | Cache de ataques WP en BD (wp_xmlrpc_hits/wp_wplogin_hits, ventana 24h, cron 3h) para la tabla admin del tab WordPress en Seguridad; primer análisis al aplicar | 2026-07-01 |
| 0083 | postscreen (portero anti-bot) en SMTP 25: corta bots por comportamiento (pregreet/pipelining/basura) antes de sondear buzones; sin DNSBL (RBL las hace Rspamd vía unbound); validación+auto-reversión | 2026-07-01 |
| 0085 | Plugins webmail Roundcube: markasjunk (botón Spam/No-spam, mueve a Junk→lo aprende el imapsieve), zipdownload, archive, attachment_reminder | 2026-07-01 |
| 0084 | uvicorn --limit-max-requests 500→50000 y RestartSec 10→2 en svqpanel.service: el worker se reciclaba cada pocos minutos por el polling del dashboard y el corte de ~14s hacía que el frontend mostrara "la API no responde (no es JSON)" sin motivo | 2026-07-01 |
| 0086 | Zona horaria por defecto del webmail (Roundcube) = Europe/Madrid: sin esto usaba 'auto' (UTC) y las fechas salían 1-2h atrasadas; el usuario puede sobreescribirla en Ajustes | 2026-07-01 |
| 0087 | real_ip de Cloudflare en nginx (set_real_ip_from + CF-Connecting-IP): sin esto nginx veía la IP de CF y no la real → el rate-limit por IP (fuerza bruta wp-login) era esquivable y daba falsos positivos, y fail2ban/CrowdSec baneaban a Cloudflare. Escribe conf.d global + cron mensual de rangos | 2026-07-01 |
| 0088 | Cierra el bypass //xmlrpc.php (el bloqueo `location = /xmlrpc.php` era match exacto y NO normaliza barras: //xmlrpc.php se colaba y arrancaba WordPress, ~18k hits/min tumbando la CPU) → regex `~ ^/+xmlrpc\.php` y `~ ^/+wp-login\.php`; regenera todos los vhosts. Y activa la jail fail2ban nginx-limit-req (estaba enabled=false) para banear al que floodea wp-login (429) | 2026-07-01 |
| 0089 | CrowdSec estaba CIEGO ante las webs de clientes (solo leía /var/log/nginx|apache2, no /home/*/web/*/logs) → no paraba la fuerza bruta DISTRIBUIDA a wp-login (decenas de IPs bajo el umbral del rate-limit por IP). Añade acquisition con los access.log de los dominios → sus escenarios http-* banean por reputación. Y bloquea /.git/ /.env /.svn en el generador de vhosts (regenera todos) | 2026-07-01 |
| 0090 | Instala la colección crowdsecurity/wordpress en CrowdSec: solo había http-wordpress-scan (escaneo de rutas), faltaba el escenario de fuerza bruta de LOGIN (http-bf-wordpress_bf) + user-enum + wpconfig. Sin él, el flood distribuido a wp-login no se baneaba aunque CrowdSec ya viera los logs (0089) | 2026-07-01 |
| 0091 | Escenario crowdsecurity/http-bf-wordpress_bf_xmlrpc (fuerza bruta vía XML-RPC): NO viene en la colección wordpress, hay que instalarlo aparte. Complementa el bloqueo de xmlrpc en nginx. (Laravel/PHP a medida no tienen colección propia; los cubren los escenarios genéricos generic-bf/sqli/xss/probing/traversal ya activos) | 2026-07-01 |
| 0092 | CrowdSec ahora lee también los ERROR.log de los dominios (nginx+apache), no solo los access.log (0089). Los 429 del rate-limit (wp-login) quedan en el error.log, no en el access → el escenario nginx-req-limit-exceeded estaba activo pero ciego. Ahora banea al que dispara el rate-limit repetidamente (visto: >1000 hits de 1 IP frenados por 429 pero sin ban) | 2026-07-01 |
| 0093 | memory_limit: php.ini global sube a 256M (techo máximo permitido por override); cada pool de dominio nace con 128M explícito (consumo contenido, solo lo sube quien lo necesite — p.ej. WooCommerce que agotaba los 128M → 500). Regenera pools + recarga FPM | 2026-07-01 |
| 0094 | message_size_limit (tamaño máx. por mensaje de Postfix) sube de 10 MB (default) a 25 MB, como Gmail. Evita rechazos "552 5.3.4 Message size exceeds fixed limit" en correos con adjuntos. Respetuoso: no baja el valor si el admin ya lo subió por encima; ajustable desde el panel (Configuración → Email). Recarga Postfix | 2026-07-01 |
| 0095 | FALLO GRAVE silencioso: /etc/nftables.conf empezaba con `flush ruleset` (borra TODAS las tablas), que destruía la tabla del firewall-bouncer de CrowdSec (ip crowdsec) en cada recarga → el bouncer entraba en bucle netlink y CrowdSec DEJABA de aplicar baneos al firewall (detectaba pero no bloqueaba; llevaba todo el día roto). Fix: flush selectivo (table+delete de inet svqpanel, sin tocar crowdsec) + reinicia el bouncer. + health-check en metrics_scheduler que detecta la desincronización y reinicia el bouncer (cada 10 min) | 2026-07-01 |
| 0096 | La CACHE de página NO funcionaba en modo Apache: nginx hace proxy_pass a Apache:8181 y el generador solo aplicaba fastcgi_cache (que NO funciona con proxy, necesita proxy_cache) → dominios con caché activada no cacheaban (WooCommerce/WP pesado → picos CPU). Fix: proxy_cache_key global + zona proxy_cache_path en modo Apache + directivas proxy_cache en el location de proxy (mismas exclusiones $skip_cache). Y BUG: regenerate_all_vhosts apagaba la caché (fastcgi_cache_enabled default False→None=lee BD, igual que xmlrpc). Regenera vhosts | 2026-07-01 |
| 0097 | SEGURIDAD: el Redis global (backend de Rspamd: Bayes/greylist/ratelimit de correo) escuchaba en 127.0.0.1:6379 SIN contraseña y disable_functions no bloquea sockets → el PHP de cualquier cliente podía hacer FLUSHALL (borrar el Bayes entrenado o vaciar su rate-limit de envío). requirepass (clave en /etc/svqpanel/redis_rspamd.pass) propagado a Rspamd, con verificación | 2026-07-02 |
| 0098 | Redis por dominio (caché de objetos WP/WooCommerce/Laravel): instala phpredis en todas las versiones PHP + garantiza el binario redis-server (sin correo, instancia global apagada). Las instancias se crean desde el panel (tab PHP): unidad systemd propia como el usuario del dominio, socket unix en private/ (0700, solo su PHP), maxmemory acotado, sin persistencia | 2026-07-02 |
| 0099 | Correo IPv6 y PTR: (a) fija la IPv6 de salida GLOBAL (smtp_bind_address6) a la del hostname (con PTR), no una del /64 que elige `ip route get` sin PTR → Gmail rechaza 550 5.7.25; (b) IPv6 dedicada por dominio ahora es OPT-IN: con pref=ipv4 (default) NO se declara el bind6 en master.cf (evita rebotes por IPv6 sin PTR); solo los dominios que eligen ipv6 explícitamente la usan. Reaplica IPs de salida de todos los dominios | 2026-07-02 |
| 0100 | SPF: añade la IPv6 GLOBAL del servidor (por la que sale el correo, con PTR) al registro SPF de las zonas existentes. Antes el SPF solo listaba la IPv6 dedicada del dominio (o ninguna) → el correo que salía por la IPv6 global daba SPF fail en Gmail. Vía backfill_dns_ipv6 | 2026-07-02 |
| 0101 | FIX del 0100: el SPF debe listar la IPv6 por la que SALE el correo (global del servidor, con PTR), NO la IPv6 dedicada/web del dominio (sin PTR → SPF fail). sync_aaaa ya no toca el SPF; lo gobierna la preferencia de salida de correo. Re-ejecuta el backfill corregido | 2026-07-02 |
| 0102 | Bad-bots: bloquea scrapers de IA/SEO agresivos que pasaban sin bloquear (meta-externalagent, PerplexityBot, DataForSeoBot, Scrapy, Timpibot, Bytedance). NO toca Applebot ni facebookexternalhit (SEO Apple + previews WhatsApp/FB/IG). Vía ensure_catalog_bots_blocked (preserva lo que el admin ya tuviera) | 2026-07-02 |
| 0103 | Anti-webshell WordPress: bloquea ejecución de PHP en wp-content/uploads en TODOS los vhosts (nginx Y Apache, en el vhost no en .htaccess que nginx ignora). Regenera los vhosts existentes | 2026-07-03 |
| 0104 | wp-cron de la flota como CronJob del panel (visible en /crons, no crontab crudo): CLI optimize_all_wp_cron limpia crons viejos y los recrea como CronJob por dominio; DISABLE_WP_CRON + cron 5min. Baja picos de CPU. 5 wp-config sin ancla pendientes | 2026-07-03 |
| 0105 | wrapper svq-cron-run silencia la salida de EXITO (solo reemite si exit!=0): un job correcto que imprime (wp-cli "Success:...") generaba un email por ejecucion via MAILTO del cron -> miles de correos del usuario a si mismo, frenados por ratelimit y apilados en cola. El historial del panel sigue registrando todo. Reinstala wrapper + purga cola de correos de cron | 2026-07-04 |
| 0106 | Limpia vhosts de webmail huérfanos: al eliminar un dominio de correo entero quedaba el vhost webmail.{dominio} como placeholder 503 para siempre (llamaba a remove() en vez de destroy()). Código corregido + este update pasa el saneador clean_orphan_vhosts en servidores ya instalados | 2026-07-04 |
| 0107 | HELO propio en transportes de salida con IP dedicada: saludaban con el hostname del servidor → SPF_HELO_SOFTFAIL y PTR↔HELO roto; ahora smtp_helo_name=mail.{dominio} cuando el bind ≠ IP global. Regenera la sección SVQPANEL_SMTP_BIND | 2026-07-04 |
| 0108 | Normaliza los TXT importados de backups (comillas dentro del contenido, trozos "a" "b", \; escapados) que rompían silenciosamente los buscadores del panel (ip6 en SPF al cambiar preferencia, detector de SPF duplicado); resincroniza las zonas afectadas. El importador ya normaliza al importar | 2026-07-04 |
| 0109 | Escalona los wp-cron de la flota: estaban todos en '*/5' → los N WordPress arrancaban su wp-cron en el MISMO minuto (thundering herd) y el load subía a >5. Ahora cada dominio usa un minuto repartido por su id (cada 10 min), sin pico. CLI restagger_wp_cron reescribe CronJob + crontab | 2026-07-04 |
| 0110 | Sube mail_max_userip_connections de Dovecot de 10 a 50 (drop-in 99-svqpanel-limits.conf): el cupo es por buzón+IP y en oficinas tras NAT todos los PCs comparten IP — con 3-4 equipos usando el mismo buzón el default se agota ("Maximum number of connections from user+IP exceeded") | 2026-07-06 |
| 0111 | El correo ya no se duplica en /var/log/syslog (rsyslog mail fuera del catch-all): CrowdSec leía cada fallo de login DOS veces (mail.log + syslog) y baneaba con la mitad de los fallos reales — una oficina entera caía con 2 intentos de un equipo con contraseña caducada | 2026-07-06 |
| 0112 | Los adjuntos por webmail (Roundcube) fallaban con límite ~2 MB aunque el correo admitiera 25 MB: el adjunto pasa por nginx→PHP antes de Postfix y esas capas seguían en su default (PHP upload_max_filesize=2M, nginx client_max_body_size 1 MB). Ahora las 3 capas (nginx+PHP+Roundcube) acompañan al message_size_limit de Postfix + margen base64. CLI sync_webmail_upload_limit | 2026-07-08 |
| 0113 | CrowdSec baneaba al admin legítimo de un WordPress mientras editaba con Elementor: los plugins disparan ráfagas de GET a /wp-json/ que dan 404 (p.ej. Elementor Pro pidiendo rutas de licencia inexistentes) y el escenario http-probing lo cuenta como sondeo. Parser de whitelist que exime /wp-json/ del probing; probes reales (.env, .git, /vendor, /wp-admin/xxx) siguen baneándose (verificado con cscli explain) | 2026-07-08 |
| 0114 | Techo global de memory_limit de PHP 256M→512M (todas las versiones FPM+CLI). Es el cap que el panel permite pedir por dominio; Elementor agota los 128M por defecto al abrir el editor (PHP Fatal: memory exhausted → 500 en admin-ajax.php) y con el cap en 256M no se podía subir más. No baja el valor si ya era mayor. Idempotente | 2026-07-08 |
| 0115 | Techo global de max_execution_time (y max_input_time) de PHP 30s→120s (todas las versiones FPM). El editor de Elementor con packs de widgets tarda >30s en cargar y el default mataba el proceso a mitad → el editor se queda "cargando" y ofrece "modo seguro". nginx (300) y Apache (300) ya cubrían el margen. Es el cap para overrides por dominio. No baja si ya era mayor. Idempotente | 2026-07-08 |
| 0116 | Subidas >1MB fallaban con "Respuesta inesperada del servidor": los vhosts nginx del panel no ponían client_max_body_size → nginx cortaba con 413 (default 1MB) antes de llegar a PHP; el 413 no es JSON y WP mostraba ese mensaje. Además upload_max_filesize/post_max_size global en 2M/8M. Ahora el generador de vhosts inyecta client_max_body_size 64m (regenera todos) + cap PHP a 64M. En Hestia no pasaba porque sí lo ponía | 2026-07-08 |
| 0117 | Los subdominios importados de HestiaCP quedaban con `Domain.ipv6` NULL aunque el importador SÍ les publicaba un AAAA en la zona del padre: el panel los mostraba "sin IPv6" mientras respondían por IPv6. Si la IPv6 del padre cambiara, el panel no actualizaría ese AAAA (cree que no tiene IPv6) y quedaría apuntando a una IP inexistente. `hestia_import.py` ahora guarda `d.ipv6` al publicar el AAAA; este update sincroniza los ya importados copiando el AAAA existente. No inventa registros DNS ni toca BIND/nginx | 2026-07-09 |
| 0118 | Escenario CrowdSec propio (svqpanel/http-444-flood): el catálogo bad-bots de nginx corta a los bots conocidos con 444 (cierra sin responder, no llega a PHP), pero los escenarios http-* de CrowdSec miran 200/403/404, NO el 444 → un bot cortado reconectaba ~1/seg sin fin (visto: IP de AWS con ~2000 hits 444 a /robots.txt). Ahora una IP que acumula muchos 444 se escala a ban de firewall (nftables). Aplica a toda la flota | 2026-07-10 |
| 0119 | En Debian 13 (Dovecot 2.4) el webmail/IMAP daba error SOLO en la bandeja de entrada ("Failed to autocreate mailbox: Permission denied"), mientras Enviados/Archive sí funcionaban. Causa: el paquete Dovecot 2.4 de Debian deja su config MBOX por defecto activa (`mail_driver=mbox`, `mail_inbox_path=/var/mail/%{user}`) conviviendo con la de SVQPanel (maildir): las subcarpetas se leen de `~/` pero el INBOX se busca en `/var/mail/` (inexistente). Comenta la config mbox de Debian en 10-mail.conf. Solo Debian 13+ | 2026-07-11 |
| 0120 | La tarjeta "Antivirus de correo (ClamAV)" salía como "ClamAV no está disponible": el install.sh nunca instalaba el paquete, aunque el panel tiene toda la lógica (antivirus_manager.py). Instala clamav + clamav-daemon + clamav-milter + clamav-freshclam y descarga las firmas. Solo si hay correo (Postfix) | 2026-07-11 |
| 0121 | El correo saliente por IPv6 usaba una IPv6 SLAAC aleatoria del /64 (Postfix sin smtp_bind_address6) sin PTR ni en el SPF → Gmail spf=fail / riesgo de rechazo. NO se apaga IPv6: se FIJA smtp_bind_address6 a la IPv6 del panel (panel_ipv6, la ::1 del rango, la misma que va al SPF). Ahora activar IPv6 en el panel lo fija solo; este update lo aplica a los que ya la tienen. La vista Salud de correo avisa si falta el PTR de esa IPv6 | 2026-07-11 |
| 0122 | Un Nextcloud en SVQPanel salía con la página "Avisos de seguridad y configuración" llena de errores que eran culpa del PANEL: faltaban los redirects `.well-known/caldav|carddav` en el vhost nginx ("su servidor no está configurado correctamente para resolver...") y `apc.enable_cli=0` hacía que `occ`/`cron.php` petaran con "Memcache APCu not available". Activa apc.enable_cli en todas las versiones PHP y regenera los vhosts con los redirects. El autoinstalador de Nextcloud ahora deja además Redis, trusted_proxies, región y ventana de mantenimiento configurados | 2026-07-11 |
| 0123 | El OPcache se quedaba con los defaults de Debian (`memory_consumption=128`, `interned_strings_buffer=8`, `max_accelerated_files=10000`), cortos para cualquier app PHP seria: al desbordarse, el OPcache desaloja clases en caliente o deja de cachear ficheros nuevos **en silencio** (el sitio va lento y nadie se entera). Nextcloud lo denuncia con "El módulo PHP OPcache no está configurado correctamente". Drop-in propio `99-svqpanel-opcache.ini` por versión de PHP con 256M / 16 / 20000 (`save_comments=1`, obligatorio para las anotaciones de Doctrine). Reversible borrando el drop-in | 2026-07-11 |
| 0124 | `ffmpeg` no se instalaba nunca → ningún Nextcloud del panel podía generar la miniatura de un vídeo (la app Fotos pedía `/apps/photos/api/v1/preview/{id}` y respondía **404** para cada MP4/MOV: galería con las fotos bien y todos los vídeos en gris). ⚠️ Este update solo pone `ffmpeg`: el proveedor `OC\Preview\Movie` lo invoca por `exec()`, que los pools del panel bloquean por política — para tener miniaturas de vídeo hay que además relajar el hardening de ESE dominio (`Domain.php_hardening_relaxed`). Las miniaturas de imagen (jpeg/png/HEIC) funcionan siempre (imagick/GD, sin exec) | 2026-07-11 |
| 0125 | SRS: postsrsd se configuraba con el dominio RAÍZ del servidor () en vez de con su hostname completo. Con un solo servidor funciona, pero lo normal es tener **varios servidores como subdominios del mismo dominio** (svq1/svq2.svqhost.red): todos reescribían el envelope-from de los reenvíos al MISMO svqhost.red, que solo puede tener un SPF → el segundo servidor sale con **spf=fail** y Gmail/Outlook rechazan sus reenvíos de alias. Ahora cada servidor usa su hostname (autónomo: montar otro no obliga a tocar los demás). ⚠️ El update solo AVISA: cambiarlo exige publicar antes SPF+DKIM del subdominio en el DNS (externo), o los reenvíos saldrían con un dominio SIN SPF | 2026-07-12 |
| 0126 | **El antivirus de correo no escaneaba NADA**, en dos frentes: (1) ClamAV cargaba ~1GB de firmas en RAM, el **OOM killer lo mataba** y la unit de Debian trae `Restart=no` → se quedaba MUERTO sin avisar y el correo dejaba de escanearse en silencio durante horas (visto: 6h caído); (2) `clamav-milter` corría pero **Postfix no lo tenía en `smtpd_milters`** (solo el de Rspamd): ClamAV estaba de adorno. Fix: MaxThreads 12→2 y ConcurrentDatabaseReload=false (al recargar firmas cargaba DOS bases en RAM: pico ×2), MemoryMax=900M + Restart=on-failure, y engancha el milter vía `antivirus_manager.enable_milter()` preservando el de Rspamd | 2026-07-12 |
| 0127 | **La jail `nginx-limit-req` de fail2ban NO ha baneado NUNCA** (Total banned: 0 desde el update 0088): su `logpath` apuntaba a los ACCESS logs, pero el filtro solo casa con `limiting requests, excess:` que nginx escribe en el **ERROR log**. Medido en el ataque a corosantamaria.org (jul 2026): 218 respuestas 429, 202 líneas `limiting requests`… y 0 baneos — 1.743 POST a wp-login.php desde 612 IPs hasta acertar la contraseña. Reescribe SOLO ese bloque de `jail.local` hacia `nginx.error.log` (no toca `svqpanel-scanner`, que usa access logs legítimamente) y aplica con `fail2ban-client reload` (NUNCA restart: perdería los baneos activos; reload además re-expande los globos, con lo que entran los sitios creados tras el último arranque). `create_domain()` ahora hace ese reload al dar de alta cada dominio | 2026-07-27 |
| 0128 | **DKIM automático**: Outlook/Hotmail y Gmail exigen SPF+DKIM+DMARC desde mayo 2025, pero el panel solo publicaba SPF y DMARC al activar el correo — el DKIM dependía de que alguien pulsara el botón de la pestaña DKIM (13 de 47 dominios del primer servidor estaban sin firma). Desde v0.214.0 el alta de correo genera el DKIM sola; este update hace el backfill (`api.cli backfill_dkim`) sobre los dominios existentes con `dkim_enabled=false`: genera la clave (o reutiliza la que hubiera en disco para no invalidar un TXT ya publicado fuera), la declara en selectors.map y publica el TXT si la zona DNS está en el panel | 2026-07-28 |
| 0129 | **Roundcube (webmail) no se actualizaba NUNCA**: no viene de apt — el install lo baja como tarball de GitHub a `/var/www/roundcube`, así que ni `apt upgrade` ni `unattended-upgrades` ni el `update.sh` del panel lo tocan. Un servidor instalado hace meses se queda clavado en la versión de aquel día, acumulando CVEs y sin aparecer en ninguna pantalla del panel. Roundcube 1.7.2 / 1.6.17 (2026-07-05) arreglan **CVE-2026-54433, un XSS almacenado *zero-click*** en el render de texto plano (se dispara con solo ABRIR un correo malicioso), CVE-2026-54432 (XSS vía MIME type de adjunto), inyección de sesión en el plugin password, un bypass SSRF y un DoS por bucle infinito en el decoder TNEF (`winmail.dat`, que Outlook manda a diario). Delega en `scripts/roundcube_updater.py` (mismo módulo que el botón del panel): backup de ficheros + dump de BD → `bin/installto.sh -y` oficial (preserva config/plugins/skins, migra la BD) → reaplica el guard de PHP 8.4/8.5 → verifica por HTTP contra un vhost real de webmail → **revierte solo** si deja de responder. Idempotente: si ya está en la última, no toca nada | 2026-07-29 |
| 0130 | **El antispam dejaba pasar sextorsión con el remitente FALSIFICADO del propio dominio** (`from: info@midominio → to: info@midominio`). El dominio tenía SPF `-all` y DMARC `p=quarantine`: debía rechazarse, y de hecho Rspamd puntúa ese correo con **23.50 → reject** cuando se le pasa a mano. Lo que falló fue el DNS: unbound (el resolver del antispam, update 0061) se quedó con la config por defecto — **1 hilo y cola de 64** —, y en una ráfaga de spam la cola se desborda y descarta consultas (medido en producción: `total.requestlist.exceeded=1445`). Sin respuestas de SPF/DMARC/RBL, Rspamd puntúa como si esos checks no existieran: el correo que se coló hizo **2 consultas DNS en 8.013 ms → 6.15 puntos → ENTREGADO**, mientras otros de la MISMA campaña (mismo monedero bitcoin) hacían 23-32 consultas en ~76 ms y salían con 24-33 → **reject**. Sube unbound a 2-4 hilos (según `nproc`), cola 2048/hilo, cache 64m/128m, `serve-expired` y buffers de 4m; y relaja el DNS de Rspamd de `timeout 1s ×5 reintentos` a `3s ×2` (1s era tan justo que con la caché fría abandonaba antes de resolver). Valida con `unbound-checkconf` y **revierte a la config mínima** si no valida | 2026-07-30 |
| 0131 | Remata el 0130: al revisar los logs de unbound DESPUÉS de aplicarlo, **dos de sus opciones no surtían efecto**. (1) `so-rcvbuf 4194304 was not granted. Got 425984` — los buffers de socket los limita el kernel (`net.core.rmem_max/wmem_max`, por defecto 208k), así que unbound pedía 4m y se quedaba en ~416k; y los buffers son justo lo que amortigua las **ráfagas** de consultas, el escenario que causó el bug original. Se sube de forma persistente vía `/etc/sysctl.d/99-svqpanel-unbound.conf`. (2) `subnetcache: serve-expired is set but not working` / `prefetch is set but not working` — Debian activa el módulo **subnetcache** (EDNS Client Subnet) por defecto y ANULA `serve-expired` y `prefetch` para lo que él cachea, que son las dos opciones que evitan esperas en los picos; ese módulo sirve para DNS con respuestas por geolocalización y aquí sobra (`module-config: "validator iterator"`). El update solo añade el sysctl y **reaplica el 0130** (idempotente) en vez de duplicar la config, y verifica que los avisos desaparecen | 2026-07-30 |
| 0132 | **El Bayes del antispam se envenenaba solo**: el sieve `learn-flag` (0036) trataba el flag `NonJunk` como "el usuario dice que es legítimo" → `learn_ham`. Pero el filtro antispam PROPIO de Thunderbird pone ese flag AUTOMÁTICAMENTE a cada correo nuevo que su clasificador cree bueno: cada spam que Thunderbird no reconocía entrenaba el Bayes del servidor como ham **un segundo después de entregarse** (medido en producción: la misma campaña de spam pasó de 5.54 a 2.22 puntos en horas, con Bayes al 67% en un spam de manual). El usuario lo movía a Junk y rectificaba… pero Thunderbird des-enseñaba con el siguiente. Fix: el flag solo entrena SPAM (Junk); el ham queda en vías humanas inequívocas (sacar de Junk) + autolearn (score ≤ 0.5). El update reaplica el sieve y **resetea el Bayes contaminado** (tokens `RS_*` + índices + `learned_ids` del Redis de Rspamd; greylist/ratelimit intactos) — se reentrena solo en días. Install ya nace bien (invoca el mismo código) | 2026-08-04 |
| 0133 | **Umbrales antispam por defecto más estrictos: greylist 3 / marcar spam 4 / rechazar 10** (antes se heredaban los de fábrica de Rspamd, 4/6/15: el spam de 4-6 puntos aterrizaba en la bandeja de entrada, visto con campañas reales a 5.54 y 2.22). A 4+ puntos casi no hay correo legítimo e ir a Junk es recuperable (sacarlo entrena el Bayes); el rechazo se queda en 10 y NO más bajo para no rebotar correo bueno un mal día del DNS (caso 0130) o del Bayes (BCC legítimo = FORGED_RECIPIENTS +2, Bayes equivocado +5.1). **Respeta a los admins que ya personalizaron** (si existe actions.conf no toca nada, vía `ensure_default_actions()`). Los servidores nuevos nacen con 3/4/10 (install.sh) | 2026-08-04 |
| 0134 | **Webmail: dos plugins más.** `newmail_notifier` (notificación de escritorio/sonido con correo nuevo; incluido en Roundcube, solo se activa) y `authres_status` (icono con el resultado SPF/DKIM/DMARC de cada correo recibido — el cliente ve al instante si un correo viene autenticado o huele a spoofing; es de terceros y se descarga FIJADO a 0.7.1, la misma versión que pone install.sh en servidores nuevos). El config se reescribe con merge idempotente + validación `php -l`; `roundcube_updater` preserva plugins/ al actualizar el webmail | 2026-08-04 |
| 0135 | Remata el 0134: `attachment_reminder` y `newmail_notifier` son **preferencias por usuario que de fábrica vienen a false** — los plugins estaban activos pero no hacían NADA hasta que cada usuario los activara a mano (descubierto en la primera prueba real: "adjunto un archivo" sin adjuntar → ningún aviso al enviar). Se activan por defecto a nivel global (`attachment_reminder = true`, `newmail_notifier_basic = true`); el usuario que no los quiera los apaga en Configuración → Preferencias | 2026-08-04 |
| 0136 | **Zonas creadas con el NS placeholder `ns1/ns2.svqpanel.local` que NO propagaban.** Al dar de alta un dominio con DNS desde `/api/domains`, la zona se generaba sin pasar ns1/ns2 y caía al placeholder histórico: el registrador delegaba en `nssvq1/nssvq2` pero la zona se declaraba autoritativa para un TLD `.local` inexistente (intoDNS: "Missing nameservers reported by parent"). El endpoint `/api/dns/zones` sí lo hacía bien, por eso solo afectaba a los dominios creados por el cliente. Fix en `api/routes/domains.py` (pasa ns1/ns2 a `create_zone`, `soa_ns` y la plantilla) + este update repara las zonas ya creadas vía `fix_placeholder_ns` (sube serial y re-empuja al cluster; sin subir el serial el esclavo las ignoraría por anti-rollback) | 2026-08-15 |
| 0137 | **El reto ACME se redirigía y rompía la emisión de SSL.** Con el dominio canónico activado (www/non-www) el vhost hacía `if ($host = dominio.com) { return 301 …www… }`, que redirigía TAMBIÉN `/.well-known/acme-challenge/`: certbot valida cada identificador en su propio nombre, así que el reto de la variante no canónica aterrizaba en la canónica, donde el token no existe → 404 y "Certbot failed to authenticate some domains". El mismo fallo en el bloque `force_https` (`return 301 https://$host`) mandaba el reto a HTTPS y rompía las RENOVACIONES automáticas. Fix: map global `$canonical_redirect` que vale `""` durante el reto (hace falta un map y no un `location` porque en nginx los `if` de fase rewrite se evalúan ANTES que cualquier location) + exención explícita en el bloque HTTP de force_https. El update crea el map (con reversión si `nginx -t` falla) y regenera los vhosts | 2026-08-15 |
| 0138 | **Correo legítimo diferido horas por postscreen** — ⚠️ **diagnóstico erróneo, lo arregla el 0139.** Se creyó que la causa era `postscreen_greet_wait` en su default `${stress?{2}:{6}}s` (6s), y se bajó a 2s. NO era la causa: acortar la espera no evita el rechazo, solo lo adelanta. Medido al día siguiente en producción: **450 rechazos (449 DESPUÉS del cambio) frente a 221 el día anterior**. Se mantiene porque 2s es un valor sano (menos latencia por conexión) y es inocuo, pero por sí solo no resuelve nada | 2026-08-20 |
| 0139 | **La causa real del correo diferido: `postscreen_greet_action = enforce`.** Con `enforce`, postscreen **rechaza a toda IP desconocida en su primera conexión** (`450 4.3.2 Service currently unavailable`) **pase o no la prueba** — el `PASS NEW` se registra justo después, cuando el correo ya se rechazó. Prueba de que no es por fallar tests: de las 445 IPs rechazadas en un día, **cero** tenían PREGREET asociado. Eso es greylisting: los emisores pequeños reintentan desde la misma IP y entran al 2º intento, pero Microsoft/Google/Hotmail reintentan **desde una IP distinta cada vez** (`52.101.94.128 → .93.77 → .90.130 → …`), así que cada reintento vuelve a ser "IP nueva" y la caché de 7d no les sirve. Fix: `greet_action = ignore` — la prueba se sigue haciendo y cacheando, pero no rechaza. **Coste aceptado**: el pregreet deja de bloquear por sí solo (~44 bots/día) a cambio de ~450 correos legítimos que dejan de retrasarse; siguen activos los otros 3 tests de protocolo (pipelining, non_smtp_command, bare_newline), que no molestan a Outlook/Gmail, más Rspamd y CrowdSec. Los servidores nuevos ya nacen con `ignore` (bloque del 0083) | 2026-08-20 |
| 0140 | **La solución real al correo diferido: allowlist de las granjas grandes en postscreen.** postscreen rechaza a toda IP desconocida en su 1ª conexión (`450 4.3.2`); los emisores pequeños reintentan desde la misma IP y entran al 2º intento, pero **Microsoft/Google/Amazon SES reintentan desde una IP distinta cada vez** (`52.101.94.128 → .93.77 → .90.130 → …`), así que nunca salen de "IP nueva" y su correo se retrasa horas. Medido en un día: de 488 rechazos, **251 (51%) eran de esas tres granjas**, y sus remitentes eran legítimos (despachos, gestorías, empresas) que acababan entregando tras varios reintentos. Los intentos previos de arreglarlo con parámetros globales fallaron (0138, 0139); esta allowlist es **quirúrgica**: exime solo a tres granjas verificadas por sus SPF oficiales (+ rDNS) y deja el portero intacto para el resto de Internet, de donde vienen los bots. El correo de esas granjas **sigue pasando** por Rspamd, Bayes, SPF/DKIM/DMARC, antivirus y CrowdSec — postscreen no filtra contenido. Rangos en `/etc/postfix/postscreen_access.cidr`; si vuelve a aparecer correo diferido de estas granjas, recomprobar sus SPF y ampliar | 2026-08-20 |
| 0141 | **Revierte el 0139**: `postscreen_greet_action` vuelve de `ignore` a `enforce`. Con `ignore` seguían apareciendo rechazos (4 en los primeros 25 min, ratio 18% — dentro del 9-33% previo al cambio), o sea que no arreglaba nada, y a cambio el pregreet dejaba de bloquear bots (~44/día): se pagaba el precio sin el beneficio. Se rearma `enforce` **solo si la allowlist del 0140 está activa** (el script lo comprueba y aborta si no), para no volver a diferir a Microsoft/Google | 2026-08-20 |
| 0142 | **Filtros de correo en el webmail (ManageSieve).** Roundcube no mostraba el botón "Filtros" (reglas del tipo "si viene de X → carpeta Y"): teníamos `dovecot-sieve` (para el aprendizaje Bayes) pero **no** `dovecot-managesieved`, el servicio que permite EDITAR filtros desde el webmail, y el plugin `managesieve` estaba en disco pero sin activar. Dos gotchas de Dovecot 2.4: (1) el servicio **no arranca** si `sieve` no está en `protocols`, aunque el paquete esté instalado; (2) hay que declarar el `sieve_script` de **tipo `personal`** — sin él ManageSieve guarda los filtros en un script que **nunca se ejecuta en la entrega**, así que el cliente crea reglas, las ve guardadas y no hacen nada. Listener **solo en 127.0.0.1:4190** (lo consume Roundcube, que corre en la misma máquina): sin exponer a Internet, sin firewall que abrir ni jail de fail2ban. Los scripts globales `before` (spam→Junk, learn-spam/ham) se ejecutan ANTES que el personal, así que el antispam y el Bayes siguen mandando sobre los filtros del usuario. Valida con `doveconf -n` **antes** de reiniciar y revierte el drop-in si la config queda inválida (Dovecot roto = todo el correo caído). **Auto-respuesta y filtros CONVIVEN**: antes ambos peleaban por `~/.dovecot.sieve` y el que escribía último borraba al otro (abrir la pestaña Filtros desactivaba la auto-respuesta; activar una auto-respuesta desde el panel borraba los filtros). Ahora la auto-respuesta va a un `sieve_script` propio de tipo `before` y el personal queda libre para el usuario. **Los Sieve salen del maildir**: el home del buzón y el maildir son el MISMO directorio (`mail_path = ~/`) y en Maildir++ toda entrada que empieza por `.` es una CARPETA DE CORREO — con los scripts dentro, al cliente le salía una carpeta fantasma `dovecot.sieve` y `fileinto` intentaba escribir en `.dovecot.sieve/tmp`, **la entrega fallaba con `451 4.2.0` y el correo se quedaba en cola**. Pasan a `/var/lib/dovecot/sieve-users/%{user}/`. El update **migra** las auto-respuestas existentes (en el .71 eran 3, dos de un cliente real con las vacaciones activas). Gotcha: `sievec` debe correr **como vmail** — compilado por root, el `.svbin` queda ilegible para Dovecot y la entrega falla igual | 2026-08-25 |
| 0143 | **Correo de clientes rebotando cuando la dirección venía en MAYÚSCULAS.** Medido en el .71: **175 correos perdidos entre el 26-jul y el 26-ago (~5/día)** con `550 User doesn't exist` aunque el buzón SÍ existía — `GLOBATEL@GLOBATEL.ES`, `JOSE.GARCIA@INMOBILIARIACAMARON.COM`, `JUCARROBE@CORIAMAGICA.ES`… de emisores reales (proveedores, Iberdrola, BBVA). El remitente recibía el rebote, así que **el cliente ni se enteraba de que le habían escrito**. CAUSA: el drop-in `99-svqpanel-lmtp.conf` del update 0062 forzaba `auth_username_format = %{user}` para quitar el `| username` del `20-lmtp.conf` de Dovecot 2.4 (que RECORTA el @dominio y sin cuyo arreglo no entra nada de correo) — correcto, salvo que al escribir `%{user}` a secas se perdió también el `| lower` del valor original, que es el que normaliza a minúsculas; y como nuestro drop-in es `99-` gana sobre el del paquete. FIX: `%{user | lower}` (email completo + minúsculas). Reflejado también en `install.sh`, `install_mail.sh` y en el propio 0062, para que un servidor que se ponga al día desde cero no reintroduzca el bug. Gotcha al escribir el update: el nombre del ajuste **contiene** la palabra `username`, así que comprobarlo con un grep sobre la línea entera da siempre positivo — hay que mirar sólo el valor a la derecha del `=` | 2026-08-26 |
| 0144 | **El wp-cron se quedaba con la versión VIEJA de PHP al cambiar la del dominio.** `wp_cron_command()` usa `domain.php_version`, pero sólo se llama **al crear** el CronJob: si luego se cambiaba el PHP del dominio, nadie actualizaba el comando. Y si la versión vieja ya no cumple el mínimo de WordPress, el cron **falla en cada ejecución** → el wp-cron del sitio deja de correr **en silencio** (con `DISABLE_WP_CRON=true` tampoco hay disparo por visitas que lo salve) y encima cron manda el error por correo cada 10 min, engordando la cola de Postfix. Caso real en el .71: `pacobasallote.com` con el dominio en PHP 8.4 y el cron en `php7.3` → *"WordPress requires at least 7.4"*, **35 tareas atascadas y 62 correos encolados**; `sonibel.es` en `php8.2` con el dominio en 8.5. FIX de código: `wp_manager.sync_wp_cron_php()`, llamado desde los DOS sitios que cambian la versión (`PUT /domains/{id}/php` y `PUT /domains/{id}`) — actualiza el CronJob y regenera la línea del crontab; no lanza excepción, un fallo ahí no debe tumbar el cambio de PHP. Este update arregla los que YA estaban desfasados | 2026-08-26 |
| 0145 | **Correo legítimo perdido: postscreen cobraba un "peaje de entrada" a cada IP.** Con `postscreen_greet_action = enforce`, postscreen rechaza (`450 4.3.2`) la 1ª conexión de toda IP que no tenga en caché **pase o no las pruebas** — el veredicto se registra DESPUÉS del rechazo. Medido en un mes de producción: **930 `PASS NEW` y 953 rechazos**, uno por uno. Y como `cache_retention_time = 7d`, quien escribe cada 8 días lo paga siempre: la **misma IP de PayPal** (`173.0.84.1`) fue rechazada el 5-sep y aceptada el 9-sep. Correo real con **cero entregas** en un mes: Netflix (24 rechazos), OVH (19, y son avisos de NO entrega), TrendMicro/pctcartuja (12), PayPal (10), Openbank (9), MailChannels/facoan (8), Ayto. de Sevilla. Todos códigos 4.x, así que **el remitente nunca recibió rebote y creía haberlo enviado**. La allowlist del 0140 era un parche que no escala: el problema no son "las granjas que rotan IP" sino cualquier IP con la caché caducada. Fix: `greet_action = ignore` — la prueba se sigue haciendo y cacheando, solo deja de rechazar por no conocer la IP. Es el **default de Postfix** y lo que hacen cPanel, Plesk, Mail-in-a-Box e iRedMail; `enforce` es config de admin experto para servidor bajo ataque, no para hosting compartido. Siguen en `enforce` los otros 3 tests, que son los que cazan bots (pipelining 120, non_smtp_command 124, bare_newline 134 bloqueos/mes) + Rspamd, Bayes, SPF/DKIM/DMARC y CrowdSec. ⚠️ **NO volver a poner `enforce`** (el 0141 lo hizo y fue un error) | 2026-09-22 |
| 0146 | **Greylisting desactivado por defecto: pasa a opt-in.** La teoría dice que una lista gris "solo retrasa" el correo, pero medido un mes en producción **ninguno de los 23 correos diferidos llegó a entregarse jamás** (cofund.solutions 12→0, preciosbajos.store 8→0, shiply-office 4→0, facoan.es 2→0, Mailchimp 2→0, digimobil 1→0). Un retraso que no termina es un bloqueo silencioso, y con código `4.7.1` el remitente **nunca recibe rebote y cree que lo envió** (el caso que lo destapó: un despacho respondiendo a un cliente; se supo 2 días después y por casualidad). Y encima **no aportaba detección**: de los 23, 22 eran spam real pero con score 6.5-7.9, o sea que ya iban a Junk por su propia puntuación (umbral `add header` = 4.00); en cambio sí atrapó legítimos con score 4.33, 4.37 y hasta **0.00** (limpio del todo). El greylisting es de 2003, anterior a Bayes y SPF/DKIM/DMARC; aquí ya hay 19 RBLs, Bayes, fuzzy (Pyzor/Razor), antivirus y CrowdSec, y ni cPanel ni Plesk lo traen activado de serie. El update pone `greylist.conf → enabled = false` y `settings.greylisting_enabled = false` (para que la UI no mienta); **no toca los ajustes por dominio**. Quien lo quiera lo activa en Ajustes o por dominio. Instalaciones nuevas nacen ya apagadas (`install.sh` + DEFAULT FALSE en el modelo y el ALTER) | 2026-09-22 |
| 0147 | **Retirada la allowlist de postscreen del 0140** (y el 0140 queda neutralizado). Era un parche sobre un diagnóstico incompleto: se creyó que el problema eran "las granjas que rotan IP", cuando postscreen con `greet_action = enforce` rechazaba la 1ª conexión de **cualquier** IP fuera de su caché, o sea Internet entero. Con el 0145 (`ignore`) la causa está atacada, así que la lista no cambia nada y solo es deuda: rangos IP que caducan, y una lista obsoleta acaba eximiendo a IPs que ya no son de quien creíamos. `postscreen_access_list` vuelve a `permit_mynetworks` (las redes propias siguen exentas) y se borra el `.cidr`. **Guard**: si `greet_action` no está en `ignore`, el update no hace nada — quitar la lista con `enforce` activo volvería a diferir a Microsoft/Google. Lección que sí conviene recordar del 0140: al copiar rangos de un SPF, copiarlo **entero** (se pusieron 2 de los 12 de Amazon SES y se coló un rechazo real en un /18 contiguo) | 2026-09-22 |
| 0148 | **`known_senders`: premiar al remitente conocido en vez de castigar al desconocido.** Es el complemento natural del 0146: donde el greylisting penalizaba a quien escribía por primera vez (y acabó bloqueando correo con score **0.00**), este módulo recuerda a los remitentes con los que ya se ha intercambiado correo y les **baja** la puntuación (`KNOWN_SENDER = -1.0`). Así el correo de la gestoría, el cliente habitual o el banco deja de rozar el umbral de Junk (4.0), sin llegar a blanquear spam real (6-8) ni tocar el de rechazo (10). **No abre ningún agujero**: a un remitente desconocido se le juzga exactamente igual que antes. ⚠️ **A propósito NO se define `symbol_unknown`** aunque el ejemplo oficial lo traiga — añadir puntos al que escribe por primera vez sería repetir el error del greylisting, y en hosting compartido el correo de un cliente nuevo, un juzgado o una administración viene siempre de un "desconocido". Config: 100k remitentes (LRU), TTL 30d, sin bloom (RedisBloom no está instalado; los sets normales además son exactos). Requisitos verificados antes de aplicar: Rspamd 4.1.1 y Redis ya en uso por Bayes/fuzzy (6,25 MB). Los servidores nuevos nacen con él (`install.sh` + este update, que añade el peso a `groups.conf`) | 2026-09-22 |
| 0149 | **Umbral de rechazo 10.00 → 6.00** (el de marcado sigue en 4.00, así que la franja 4-6 continúa yendo a Junk). Decisión del administrador: el spam evidente no debe ocupar la carpeta de No deseado del cliente, debe rebotarse. Comprobado antes de aplicar: **todo** lo que había en la franja 6-10 era spam inequívoco — `preciosbajos.store` (×11), `cofund.solutions` (×11), `artio-host.shop`, `notificamailerb.com`; dominios desechables y remitentes con nombre aleatorio (`ucqomdm`, `ywxedpd`, `iwfillh`). Ni un legítimo. ⚠️ **Riesgo asumido y documentado**: un rechazo es DEFINITIVO, mientras que Junk es recuperable (y sacarlo de ahí entrena el Bayes). Al medir apareció un aviso: `shiply-office.com` puntuó **9.70 en un correo y 0.00 en otro** pese a tener `BAYES_HAM(-3.00)`, DKIM válido y DMARC pass — con umbral 6 ese correo se habría perdido. Conviene revisar los rechazos las primeras semanas (`grep reject /var/log/rspamd/rspamd.log | grep -v "soft reject"`). El update **respeta al admin** que ya hubiera personalizado el umbral (solo toca si está en el 10.00 por defecto) y verifica el valor efectivo con `configdump`, no solo el del fichero. Servidores nuevos nacen con 6.00 (`install.sh` + `DEFAULT_ACTIONS`) | 2026-09-22 |
| 0150 | **El 0149 dejó al Bayes sin la mitad de su alimentación: autolearn de spam 10 → 6.** Al bajar el rechazo a 6.00 (update 0149), el autolearn seguía en 10, así que **los correos de la franja 6-10 se rechazaban sin llegar a aprenderse**: 33 de los 56 casos de spam medidos (**59%**). El efecto era el contrario del buscado — menos patrones aprendidos → más spam colándose con score bajo hasta la bandeja de entrada. Lo destapó un caso real: el mismo remitente (`cofund.solutions`) puntuaba **7.15 con `BAYES_SPAM(4.92)`** y **1.60 con `BAYES_SPAM(0.00)[24.24%]`** — cuando el Bayes reconoce el patrón cae, cuando no, se cuela. ⚠️ **REGLA**: el autolearn de spam debe COINCIDIR siempre con el umbral de rechazo; si se mueve uno hay que mover el otro. Riesgo asumido: un legítimo que cayera en 6-10 se aprendería como spam y envenenaría el Bayes (pasó algo así en el 0132 con el flag NonJunk), pero en esa franja no había ni un legítimo. El update invoca `spam_learning.install()` (fuente de verdad del panel) con red de seguridad por `sed`, y verifica el valor efectivo con `configdump` avisando si autolearn y rechazo se desalinean | 2026-09-22 |
| 0151 | **`R_MIXED_CHARSET` 0.63 → 2.50**: el correo declara un juego de caracteres que no coincide con su contenido (típico: `charset=windows-1251` —cirílico— con texto en español). No ocurre por accidente: es la huella de herramientas de envío masivo configuradas en otro idioma. Se midieron todos los candidatos contra el correo **real** de dos servidores y se descartaron los demás: `MISSING_XM_UA` (176 de 228 en correo limpio), `RCVD_VIA_SMTP_AUTH` (19 limpios), `PREVIOUSLY_DELIVERED` y `DWL_DNSWL_NONE` (salen en Gmail, Stripe, Amazon, ONCE, Obramat) y `RECEIVED_SPAMHAUS_PBL` — este engaña: el **PBL no es una lista de spammers** sino de rangos domésticos, y salta porque la IP residencial del remitente queda en las cabeceras `Received`; saltó en un despacho legítimo. `R_MIXED_CHARSET` en cambio: **13 apariciones, 12 spam y 1 legítima** (un Mailchimp con 1.63) — y detecta una incoherencia **interna** del correo, no una característica de su origen. Efecto: `cofund.solutions` 1.60→3.47, `preciosbajos.store` 7.30→9.17, Mailchimp 1.63→**3.50 (sigue entrando**, Junk está en 4.00). Honestamente esto solo no cierra `cofund` (le faltan 0.53); eso debe hacerlo el Bayes con la alimentación que le devolvió el 0150. El peso va en `groups.conf` y en `BASE_WEIGHTS` para sobrevivir a los guardados del panel | 2026-09-22 |
| 0152 | **El 0145 no bastó: los deep protocol tests también cobraban peaje.** Tras poner `greet_action = ignore` seguían apareciendo rechazos: **30 `PASS NEW` = 30 `reject 450` en 2 horas** (frente a 0 en la hora anterior), incluido correo de **Google**. Causa: `pipelining`, `non_smtp_command` y `bare_newline` son **deep protocol tests** y por diseño **obligan al cliente a reintentar la entrega**, porque para ejecutarlos hay que romper la sesión SMTP — pase o no el correo la prueba. Es decir, reintroducían el mismo peaje que el 0145 quitaba. El `pregreet` NO es deep test (se resuelve en la misma sesión), por eso sigue cazando bots. Fix: los tres a `ignore`. ⚠️ **No es desinstalar postscreen**: sigue en `master.cf`, sigue ejecutando los tests y cacheando veredictos; solo deja de cortar la conexión. Coste asumido: esos tests bloquearon 378 casos/mes que pasan ahora a Rspamd (Bayes, 19 RBLs, SPF/DKIM/DMARC, fuzzy, antivirus) y CrowdSec. Es la configuración de Mail-in-a-Box e iRedMail. Se corrigió además la cabecera del 0083, que afirmaba que "los grandes respetan el protocolo → no les afecta" — era falso y costó correo perdido | 2026-09-22 |
| 0153 | **La pieza que faltaba: `enable = no`, no basta con `action = ignore`.** El 0152 puso las acciones en `ignore` pero dejó los deep protocol tests **habilitados**, y ahí está el matiz que costó cinco diagnósticos: `*_action` decide qué hacer con el **resultado**; `*_enable` decide si la prueba **se hace**. Con `enable = yes`, postscreen ejecuta el test igualmente y para ello **corta la sesión SMTP**, obligando a reintentar — el rechazo ocurre antes de que `action` opine. Lo demostró comparar los **dos servidores en paralelo** tras el 0152, 2h de tráfico real: el que tenía `enable = no` hizo **93 conexiones con 0 rechazos**; el que tenía `yes` hizo 51 y **rechazó 10**, entre ellos 4 intentos de HubSpot a `mjverpin@coriaweb.es` (buzón de cliente) que nunca llegaron, rotando IP en cada reintento (`.155 → .243 → .203 → .176`). El servidor bueno ya los tenía en `no` de antes, coherente con la nota del proyecto sobre las quejas de retrasos. **Queda activo el pregreet**, que no es deep test y es el que caza bots de verdad (5 en esas mismas 2h), más Rspamd, Bayes, 19 RBLs, SPF/DKIM/DMARC, fuzzy, antivirus y CrowdSec. El update verifica que el puerto 25 siga escuchando y revierte si no | 2026-09-23 |
