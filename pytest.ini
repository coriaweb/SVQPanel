[pytest]
# Tests de SVQPanel — unitarios de validaciones/parsers críticos.
# Ejecutar desde la raíz del repo:  pytest
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
