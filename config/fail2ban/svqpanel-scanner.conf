# SVQPanel — filtro fail2ban para scanners de vulnerabilidades y exploits
# Solo captura rutas que NUNCA genera tráfico legítimo.
# Seguro frente a Googlebot/Bingbot: ninguno de estos patrones los generan.

[Definition]

failregex = ^<HOST> \S+ \S+ \[.*?\] "(?:GET|POST|HEAD|PUT|DELETE) (?:.*?)(?:eval-stdin\.php|/phpunit/|/cgi-bin/|allow_url_include|auto_prepend_file|/\.env|wp-config\.php|/shell\.php|/c99\.php|/r57\.php|/webshell|/cmd\.php|\.git/HEAD|/actuator/|/solr/|/manager/html|/hudson|/.aws/|/config\.json|/server-status|libredtail).*? HTTP/\S+" (?:400|404|405|444) .+$

ignoreregex =

datepattern = {^LN-BEG}%%ExY(?P<_sep>[-/.])%%m(?P=_sep)%%d[T ]%%H:%%M:%%S(?:[.,]%%f)?(?:\s*%%z)?
              ^[^\[]*\[({DATE})
              {^LN-BEG}
